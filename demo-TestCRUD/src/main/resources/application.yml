spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/testcruddb
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
properties:
    hibernate:
      format_sql: true
profiles:
    active: test
